package handler

import (
	"net/http"
)

func PostDishToCart(w http.ResponseWriter, r *http.Request) {
	//mySession := createCartSession(w, r)
	//var cart model.CartItem
	//
	//err := json.NewDecoder(r.Body).Decode(&cart)

}
